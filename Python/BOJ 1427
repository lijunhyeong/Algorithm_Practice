import sys      # 빠른 입출력 sys.stdin.readline().rstrip()

n = list(sys.stdin.readline().rstrip())
n.sort(reverse=True)

print("".join(n))

'''
리스트로 수를 받은 후에
내림차순 정렬을 해주고
join을 통해 각 요소를 이어 붙여서 줄력한다.
'''
