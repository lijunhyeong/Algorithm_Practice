m = int(input())
n = int(input())

# 1)
case = [ii for ii in range(m, n+1) ]

# 2)
if 1 in case:
    case.remove(1)

# 3)
for i in range(m,n+1):
    for j in range(2, i):
        if i%j==0:
            case.remove(i)
            break

# 4)
if len(case) == 0:
    print(-1)
else:
    print(sum(case))
    print(min(case))


''' explanation
1) case에 m이상 n이하 값을 넣은 리스트를 만든다.
2) case안에 1이 있으면 지운다.
3) m부터 n까지 for문을 돈다. i의 값을 2부터 i전까지 나누어서 나누어 떨어지면 
소수가 아니기 때문에 case에서 지운다.
4) 문제 조건에 맞게 출력한다.
'''
    
