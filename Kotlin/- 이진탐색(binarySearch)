// 이진탐색 기본 코드
fun binarySearch(st: Int, en: Int, target: Int){
    if (st == en) {
        //....
        return 
    }
    
    val mid = (st + en) / 2
    
    if (nList[mid] < target) {
        binarySearch(mid + 1, en, target)
    } else {
        binarySearch(st, mid, target)
    }

}
